
import Header from '../../components/header';

const App = (props) => {
    return (
        <>
            <Header title={'Notifycation'} depth={1} />
            <main>
                <div>
                    notifycation
                </div>
            </main>
        </>
    );
}

export default App;

App.defaultProps = {
    
};