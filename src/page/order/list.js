
import { Layout, Top } from '../../components';

const App = (props) => {
    return (
        <>
            <Top title={'Maintenance Order Flight'} depth={1} />
            <Layout>
                order
            </Layout>
        </>
    );
}

export default App;

App.defaultProps = {

};